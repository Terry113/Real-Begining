package p3_Jamda_expresstions;

public class demo {

	public static void main(String[] args) {
		JambaExpressions add = (x, y) -> {
			return  x + y;
			
		};

		JambaExpressions subtract = (x, y) -> {
			return  x - y;
			
		};
		
		JambaExpressions multiply = (x, y) -> {
			return  x * y;
			
		};
		JambaExpressions divide = (x, y) -> {
			return  x / y;
			
		};
	}

}
